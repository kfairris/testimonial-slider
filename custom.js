let slider = tns({
    container: '.hero__slider',
    items: 1,
    slideBy: 'page',
    // autoWidth: true,
    center: true,
    mouseDrag: true,
    // arrowKeys: true,
    // autoplay: false,
    loop: false,
    // fixedWidth: 1440,
    controls: true,
    controlsContainer: '#customize-controls',
    nav: false,
    // navPosition: 'bottom',
    // swipeAngle: false,
    speed: 400,
    // viewportMax:1440
});
